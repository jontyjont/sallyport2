<h2>Renew your password</h2>

<%= form_for(resource, :as => resource_name, :url => [resource_name, :password_expired], :html => { :method => :put }) do |f| %>
  <%= devise_error_messages! %>
  <p>
  	In order to improve security and reduce spamming your old password is no longer valid. You must use a secure password which has at least one upper case letter, at least one lower case letter and at least one digit. Your password must be between 8 and 125 characters long. If you forget your password you can renew it using the forgot password link. This will send you an email with token link to renew your password.
  </p>
  <div class='control-group'>
	   
	     <%= f.label :current_password, "Current password" %>
	  
	   <div class='controls'>
	     <%= f.password_field :current_password,:class=> 'control-label' %>
	   </div>
  </div>
  <div class='control-group'>
	   
	     <%= f.label :password, "New password",:class=> 'control-label' %>
	   
	   <div class='controls'>
	     <%= f.password_field :password %>
	   </div>
  </div>
  <div class='control-group'>
	  
	     <%= f.label :password_confirmation, "Confirm new password",:class=> 'control-label' %>
	 
	   <div class='controls'>
	    <%= f.password_field :password_confirmation %>
	   </div>
  </div>
  <div class='control-group'>
    <%= f.submit "Change my password", class: 'btn btn-inverse' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
